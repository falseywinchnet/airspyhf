[workspace]
resolver = "2"
members = [
    "libairspyhf",
    "tools",
]

[profile.dev]
opt-level = 1

[profile.release]
opt-level = 3
debug = false
codegen-units = 1
lto = "thin"

[workspace.lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)', 'cfg(loom)', 'cfg(miri)'] }
